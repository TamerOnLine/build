# Builder & Fonts

- `register_all_fonts()` before any PDF build.
- `build_resume_pdf(data=...)` supports:
  - `profile`
  - `theme_name` or `theme`
  - `ui_lang`, `rtl_mode`
  - `layout_inline` (flow/blocks)
